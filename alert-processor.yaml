apiVersion: apps/v1
kind: Deployment
metadata:
  name: alert-processor-deployment
  namespace: alert-processor-namespace
  labels:
    app: alert-processor-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alert-processor
  template:
    metadata:
      labels:
        app: alert-processor
    spec:
      containers:
        - name: alert-processor
          image: andrejp02/alert-processor
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: alert-processor-service
  namespace: alert-processor-namespace
  labels:
    app: alert-processor
spec:
  selector:
    app: alert-processor
  ports:
    - name: web
      port: 3000
      targetPort: 3000
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: alert-service-server-monitor
  namespace: alert-processor-namespace
spec:
  selector:
    matchLabels:
      app: alert-processor
  endpoints:
    - port: web
      interval: 15s
      path: /metrics

